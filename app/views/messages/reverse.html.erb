<%= render partial: 'index', locals: { users: @users, messages: @messages, title: @title } %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_message_path,
            :class => 'btn btn-primary' %>
<%= link_to 'Messages', messages_path, :class => 'btn' %>
